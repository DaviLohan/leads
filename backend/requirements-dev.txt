-r requirements.txt

pytest>=9.1.1,<10.0
pytest-django>=4.14.0,<5.0
pytest-cov>=7.1.0
ruff>=0.16.3,<0.17
black>=26.5.1,<27.0
mypy>=2.3.1,<3.0
django-stubs[compatible-mypy]>=6.1.0,<7.0
djangorestframework-stubs>=3.18.0,<4.0
factory-boy>=3.3.3,<4.0
pip-audit>=2.7
# Reinicia o worker do Celery quando o código muda. Sem isto o processo fica com a versão
# de quando subiu, e o sintoma é dado gravado pela metade — sem erro nenhum no log.
watchdog>=4.0
